{
  "frontend/src/App.tsx": "Main application entry point with TanStack Router setup for all routes; the `seedData()` call on mount now only initializes empty arrays (no hardcoded tournaments are injected).",
  "frontend/src/hooks/useQueries.ts": "Minimal placeholder file since all data operations use localStorage directly via seedData utilities rather than backend calls.",
  "frontend/src/pages/HomePage.tsx": "Homepage with hero section, stats counter, features grid, and CTA banner — the \"Active/Featured Tournaments\" section and its associated polling interval and storage event listener have been fully removed.",
  "frontend/src/pages/PaymentPage.tsx": "Payment page displaying UPI ID, a static QR code image (replacing the qrcode.js CDN-generated canvas), and screenshot upload — uses `useSearch({ strict: false })` to read search params from the nested layout route context.",
  "frontend/src/pages/AdminLoginPage.tsx": "Admin login page with a dark-themed form, lock icon, email/password fields authenticated against hardcoded credentials (genZeSports2026@gmail.com / GenZe@2026), storing session in localStorage on success.",
  "frontend/src/pages/AdminPanelPage.tsx": "Full admin panel with dashboard, tournament management (create/delete), player management, and payment management; now calls `notifyTournamentsUpdated()` after creating or deleting a tournament to immediately broadcast the change to all open tabs via a dispatched StorageEvent.",
  "frontend/src/pages/LeaderboardPage.tsx": "Leaderboard page showing approved players in a ranked table with podium display for top 3, game filter buttons, and rank badges — reads from localStorage.",
  "frontend/src/pages/ContactPage.tsx": "Contact page with contact info cards (email, UPI/WhatsApp, response time), social links, and a contact form with success feedback.",
  "frontend/src/pages/RegisterPage.tsx": "Player registration form that reads active tournaments from localStorage; after saving the player record and calling `incrementTournamentSlots()`, it dispatches a StorageEvent via `notifyTournamentsUpdated()` so all open tabs immediately reflect the updated slot count.",
  "frontend/src/utils/seedData.ts": "Defines Tournament, Player, and Payment interfaces plus all localStorage CRUD helpers; hardcoded sample tournaments removed so only admin-created tournaments are stored, and a new `incrementTournamentSlots(tournamentId)` helper increments a tournament's `slotsUsed` count by 1 and persists it.",
  "frontend/src/pages/TournamentsPage.tsx": "Tournaments listing page with game filter that polls localStorage every 3 seconds and listens for storage events (including same-tab dispatches) to show all admin-created active tournaments with live slot counts; heading updated to \"All Tournaments\"."
}